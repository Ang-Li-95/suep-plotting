# Datasets come from the central registry; see ../datasets.yaml.
_include: ../datasets.yaml

suep_temp2_preG4fix:
suep_temp2_postG4fix:
  _from: suep_temp2
  label: "T=2 (post-G4 fix)"
  color: "tab:red"
