# ── Category 1/5: clusters gen-matched to an LLP ──────────────────
# The only set of the five that needs gen information, so the only one whose
# derive() runs the truth steps: cluster.matched comes from llp_reco.  The
# clustering parameters are ../common's, which ../common_clustersrc pins for
# all five, so the populations stay comparable bin for bin.
_extends: ../common_gen
